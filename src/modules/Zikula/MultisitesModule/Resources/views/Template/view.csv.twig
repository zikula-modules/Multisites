{# purpose of this template: templates view csv view #}
{% spaceless %}"{{ __('Name') }}";"{{ __('Description') }}";"{{ __('Sql file') }}";"{{ __('Parameters') }}";"{{ __('Folders') }}";"{{ __('Excluded tables') }}";"{{ __('Workflow state') }}"
;"{{ __('Projects') }}"
;"{{ __('Sites') }}"
{% endspaceless %}
{% for template in items %}
{% spaceless %}
    "{{ template.name }}";"{{ template.description }}";"{{ template.sqlFile }}";"{{ template.parameters }}";"{{ template.folders }}";"{{ template.excludedTables }}";"{{ template.workflowState|zikulamultisitesmodule_objectState(false)|lower }}"
    ;"
    {% if template.projects|default %}
        {% for relatedItem in template.projects %}
        {{ relatedItem.getTitleFromDisplayPattern() }}{% if not loop.last %}, {% endif %}
        {% endfor %}
    {% endif %}
    "
    ;"
    {% if template.sites|default %}
        {% for relatedItem in template.sites %}
        {{ relatedItem.getTitleFromDisplayPattern() }}{% if not loop.last %}, {% endif %}
        {% endfor %}
    {% endif %}
    "
{% endspaceless %}
{% endfor %}
