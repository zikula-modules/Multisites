<?php
/**
 * Multisites.
 *
 * @copyright Albert P?rez Monfort (Zikula)
 * @license http://www.gnu.org/licenses/lgpl.html GNU Lesser General Public License
 * @author Albert P?rez Monfort <aperezm@xtec.cat>.
 * @link http://modulestudio.de
 * @link http://zikula.org
 * @version Generated by ModuleStudio 0.7.0 (http://modulestudio.de).
 */

namespace Zikula\MultisitesModule\Container\Base;

use Zikula\Bundle\HookBundle\AbstractHookContainer;
use Zikula\Bundle\HookBundle\Bundle\SubscriberBundle;

/**
 * Base class for hook container methods.
 */
class HookContainer extends AbstractHookContainer
{
    /**
     * Define the hook bundles supported by this module.
     *
     * @return void
     */
    protected function setupHookBundles()
    {
        $bundle = new SubscriberBundle('ZikulaMultisitesModule', 'subscriber.zikulamultisitesmodule..ui_hooks.sites', 'ui_hooks', $this->__('zikulamultisitesmodule. Sites Display Hooks'));
        
        // Display hook for view/display templates.
        $bundle->addEvent('display_view', 'zikulamultisitesmodule.ui_hooks.sites.display_view');
        // Display hook for create/edit forms.
        $bundle->addEvent('form_edit', 'zikulamultisitesmodule.ui_hooks.sites.form_edit');
        // Display hook for delete dialogues.
        $bundle->addEvent('form_delete', 'zikulamultisitesmodule.ui_hooks.sites.form_delete');
        // Validate input from an ui create/edit form.
        $bundle->addEvent('validate_edit', 'zikulamultisitesmodule.ui_hooks.sites.validate_edit');
        // Validate input from an ui delete form.
        $bundle->addEvent('validate_delete', 'zikulamultisitesmodule.ui_hooks.sites.validate_delete');
        // Perform the final update actions for a ui create/edit form.
        $bundle->addEvent('process_edit', 'zikulamultisitesmodule.ui_hooks.sites.process_edit');
        // Perform the final delete actions for a ui form.
        $bundle->addEvent('process_delete', 'zikulamultisitesmodule.ui_hooks.sites.process_delete');
        $this->registerHookSubscriberBundle($bundle);
        
        $bundle = new SubscriberBundle('ZikulaMultisitesModule', 'subscriber.zikulamultisitesmodule..filter_hooks.sites', 'filter_hooks', $this->__('zikulamultisitesmodule. Sites Filter Hooks'));
        // A filter applied to the given area.
        $bundle->addEvent('filter', 'zikulamultisitesmodule.filter_hooks.sites.filter');
        $this->registerHookSubscriberBundle($bundle);
        
        $bundle = new SubscriberBundle('ZikulaMultisitesModule', 'subscriber.zikulamultisitesmodule..ui_hooks.templates', 'ui_hooks', $this->__('zikulamultisitesmodule. Templates Display Hooks'));
        
        // Display hook for view/display templates.
        $bundle->addEvent('display_view', 'zikulamultisitesmodule.ui_hooks.templates.display_view');
        // Display hook for create/edit forms.
        $bundle->addEvent('form_edit', 'zikulamultisitesmodule.ui_hooks.templates.form_edit');
        // Display hook for delete dialogues.
        $bundle->addEvent('form_delete', 'zikulamultisitesmodule.ui_hooks.templates.form_delete');
        // Validate input from an ui create/edit form.
        $bundle->addEvent('validate_edit', 'zikulamultisitesmodule.ui_hooks.templates.validate_edit');
        // Validate input from an ui delete form.
        $bundle->addEvent('validate_delete', 'zikulamultisitesmodule.ui_hooks.templates.validate_delete');
        // Perform the final update actions for a ui create/edit form.
        $bundle->addEvent('process_edit', 'zikulamultisitesmodule.ui_hooks.templates.process_edit');
        // Perform the final delete actions for a ui form.
        $bundle->addEvent('process_delete', 'zikulamultisitesmodule.ui_hooks.templates.process_delete');
        $this->registerHookSubscriberBundle($bundle);
        
        $bundle = new SubscriberBundle('ZikulaMultisitesModule', 'subscriber.zikulamultisitesmodule..filter_hooks.templates', 'filter_hooks', $this->__('zikulamultisitesmodule. Templates Filter Hooks'));
        // A filter applied to the given area.
        $bundle->addEvent('filter', 'zikulamultisitesmodule.filter_hooks.templates.filter');
        $this->registerHookSubscriberBundle($bundle);
        
        $bundle = new SubscriberBundle('ZikulaMultisitesModule', 'subscriber.zikulamultisitesmodule..ui_hooks.siteextensions', 'ui_hooks', $this->__('zikulamultisitesmodule. Site extensions Display Hooks'));
        
        // Display hook for view/display templates.
        $bundle->addEvent('display_view', 'zikulamultisitesmodule.ui_hooks.siteextensions.display_view');
        // Display hook for create/edit forms.
        $bundle->addEvent('form_edit', 'zikulamultisitesmodule.ui_hooks.siteextensions.form_edit');
        // Validate input from an ui create/edit form.
        $bundle->addEvent('validate_edit', 'zikulamultisitesmodule.ui_hooks.siteextensions.validate_edit');
        // Perform the final update actions for a ui create/edit form.
        $bundle->addEvent('process_edit', 'zikulamultisitesmodule.ui_hooks.siteextensions.process_edit');
        $this->registerHookSubscriberBundle($bundle);
        
        $bundle = new SubscriberBundle('ZikulaMultisitesModule', 'subscriber.zikulamultisitesmodule..filter_hooks.siteextensions', 'filter_hooks', $this->__('zikulamultisitesmodule. Site extensions Filter Hooks'));
        // A filter applied to the given area.
        $bundle->addEvent('filter', 'zikulamultisitesmodule.filter_hooks.siteextensions.filter');
        $this->registerHookSubscriberBundle($bundle);
        
        $bundle = new SubscriberBundle('ZikulaMultisitesModule', 'subscriber.zikulamultisitesmodule..ui_hooks.projects', 'ui_hooks', $this->__('zikulamultisitesmodule. Projects Display Hooks'));
        
        // Display hook for view/display templates.
        $bundle->addEvent('display_view', 'zikulamultisitesmodule.ui_hooks.projects.display_view');
        // Display hook for create/edit forms.
        $bundle->addEvent('form_edit', 'zikulamultisitesmodule.ui_hooks.projects.form_edit');
        // Display hook for delete dialogues.
        $bundle->addEvent('form_delete', 'zikulamultisitesmodule.ui_hooks.projects.form_delete');
        // Validate input from an ui create/edit form.
        $bundle->addEvent('validate_edit', 'zikulamultisitesmodule.ui_hooks.projects.validate_edit');
        // Validate input from an ui delete form.
        $bundle->addEvent('validate_delete', 'zikulamultisitesmodule.ui_hooks.projects.validate_delete');
        // Perform the final update actions for a ui create/edit form.
        $bundle->addEvent('process_edit', 'zikulamultisitesmodule.ui_hooks.projects.process_edit');
        // Perform the final delete actions for a ui form.
        $bundle->addEvent('process_delete', 'zikulamultisitesmodule.ui_hooks.projects.process_delete');
        $this->registerHookSubscriberBundle($bundle);
        
        $bundle = new SubscriberBundle('ZikulaMultisitesModule', 'subscriber.zikulamultisitesmodule..filter_hooks.projects', 'filter_hooks', $this->__('zikulamultisitesmodule. Projects Filter Hooks'));
        // A filter applied to the given area.
        $bundle->addEvent('filter', 'zikulamultisitesmodule.filter_hooks.projects.filter');
        $this->registerHookSubscriberBundle($bundle);
        
        
        
    }
}
